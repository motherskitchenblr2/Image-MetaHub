name: Deploy license server

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: license-server-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: license-server-production
    defaults:
      run:
        working-directory: packages/license-server
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ vars.CLOUDFLARE_ACCOUNT_ID }}
      LICENSE_D1_DATABASE_ID: ${{ vars.LICENSE_D1_DATABASE_ID }}
      LICENSE_SERVER_URL: ${{ vars.LICENSE_SERVER_URL }}
      LICENSE_SIGNING_PUBLIC_KEY: ${{ vars.LICENSE_SERVER_PUBLIC_KEY }}
      LICENSE_SIGNING_PRIVATE_KEY: ${{ secrets.LICENSE_SIGNING_PRIVATE_KEY }}
      LICENSE_SERVER_ADMIN_TOKEN: ${{ secrets.LICENSE_SERVER_ADMIN_TOKEN }}
      EMAIL_LOOKUP_PEPPER: ${{ secrets.EMAIL_LOOKUP_PEPPER }}
      STRIPE_WEBHOOK_SECRET: ${{ secrets.STRIPE_WEBHOOK_SECRET }}
      STRIPE_RESTRICTED_API_KEY: ${{ secrets.STRIPE_RESTRICTED_API_KEY }}
      LICENSE_DELIVERY_ENCRYPTION_KEY: ${{ secrets.LICENSE_DELIVERY_ENCRYPTION_KEY }}
      RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
      STRIPE_ACCOUNT_ID: ${{ vars.STRIPE_ACCOUNT_ID }}
      STRIPE_SUBSCRIPTION_PRODUCT_ID: ${{ vars.STRIPE_SUBSCRIPTION_PRODUCT_ID }}
      STRIPE_MONTHLY_PRICE_ID: ${{ vars.STRIPE_MONTHLY_PRICE_ID }}
      STRIPE_ANNUAL_PRICE_ID: ${{ vars.STRIPE_ANNUAL_PRICE_ID }}
      STRIPE_MONTHLY_HISTORICAL_PRICE_IDS: ${{ vars.STRIPE_MONTHLY_HISTORICAL_PRICE_IDS }}
      STRIPE_ANNUAL_HISTORICAL_PRICE_IDS: ${{ vars.STRIPE_ANNUAL_HISTORICAL_PRICE_IDS }}
      STRIPE_LIFETIME_PRICE_ID: ${{ vars.STRIPE_LIFETIME_PRICE_ID }}
      LICENSE_EMAIL_FROM: ${{ vars.LICENSE_EMAIL_FROM }}
      LICENSE_EMAIL_REPLY_TO: ${{ vars.LICENSE_EMAIL_REPLY_TO }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Worker tooling
        run: npm install --ignore-scripts

      - name: Validate all production configuration before deployment
        run: node ../../scripts/prepareLicenseServerDeployment.mjs wrangler.production.generated.json

      - name: Validate Worker bundle without deploying
        run: npx wrangler deploy --dry-run --config wrangler.production.generated.json

      - name: Prepare required Worker secrets
        shell: bash
        run: |
          set -euo pipefail
          jq -n \
            --arg signingKey "$LICENSE_SIGNING_PRIVATE_KEY" \
            --arg adminToken "$LICENSE_SERVER_ADMIN_TOKEN" \
            --arg emailPepper "$EMAIL_LOOKUP_PEPPER" \
            --arg stripeWebhookSecret "$STRIPE_WEBHOOK_SECRET" \
            --arg stripeRestrictedKey "$STRIPE_RESTRICTED_API_KEY" \
            --arg deliveryEncryptionKey "$LICENSE_DELIVERY_ENCRYPTION_KEY" \
            --arg resendApiKey "$RESEND_API_KEY" \
            '{LICENSE_SIGNING_PRIVATE_KEY: $signingKey, LICENSE_SERVER_ADMIN_TOKEN: $adminToken, EMAIL_LOOKUP_PEPPER: $emailPepper, STRIPE_WEBHOOK_SECRET: $stripeWebhookSecret, STRIPE_RESTRICTED_API_KEY: $stripeRestrictedKey, LICENSE_DELIVERY_ENCRYPTION_KEY: $deliveryEncryptionKey, RESEND_API_KEY: $resendApiKey}' \
            > .production-secrets.json

      - name: Apply D1 migrations
        run: npx wrangler d1 migrations apply image-metahub-licenses --remote --config wrangler.production.generated.json

      - name: Deploy validated Worker code and public configuration
        id: deploy
        run: npx wrangler deploy --config wrangler.production.generated.json --secrets-file .production-secrets.json

      - name: Mandatory post-deploy entitlement smoke test
        run: node ../../scripts/smokeTestLicenseServer.mjs

      - name: Roll back Worker after a failed post-deploy check
        if: ${{ failure() && steps.deploy.outcome == 'success' }}
        run: npx wrangler rollback --yes --message "Automatic rollback after failed licensing smoke test" --config wrangler.production.generated.json

      - name: Remove generated deployment configuration
        if: ${{ always() }}
        shell: bash
        run: rm -f wrangler.production.generated.json .production-secrets.json
