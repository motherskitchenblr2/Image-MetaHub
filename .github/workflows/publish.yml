name: Build and publish release (All Platforms)

on:
  workflow_dispatch: {}
  push:
    tags:
      - "v*"

jobs:
  build-windows:
    name: Build Windows
    runs-on: windows-latest
    permissions:
      contents: write
      packages: write
    env:
      IMH_LICENSE_SERVER_URL: ${{ vars.LICENSE_SERVER_URL }}
      IMH_LICENSE_PUBLIC_KEY: ${{ vars.LICENSE_SERVER_PUBLIC_KEY }}
    outputs:
      release-created: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Configure public license client
        run: node scripts/configureLicenseClient.mjs

      - name: Generate release notes
        shell: bash
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "Generating release notes for version $VERSION"
          node scripts/generate-release.js $VERSION

      - name: Build Windows installer and portable app
        shell: bash
        run: |
          echo "Building Windows installer and portable app..."
          npm run build
          npx electron-builder --win --publish=never
          echo "Windows build completed!"
          ls -la dist-electron/

      - name: Create GitHub Release
        shell: bash
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          RELEASE_NOTES_FILE="release-v$VERSION.md"

          echo "Creating release v$VERSION..."
          if [ -f "$RELEASE_NOTES_FILE" ]; then
            gh release create v$VERSION \
              --title "Image MetaHub v$VERSION" \
              --notes-file "$RELEASE_NOTES_FILE" \
              --prerelease=false \
              --draft
          else
            echo "Release notes file not found, creating basic release..."
            gh release create v$VERSION \
              --title "Image MetaHub v$VERSION" \
              --generate-notes \
              --prerelease=false \
              --draft
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Windows assets
        shell: bash
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "Uploading Windows assets for v$VERSION"
          for file in dist-electron/*; do
            if [[ "$file" == *"Setup"*".exe" ]] || [[ "$file" == *"Portable"*".exe" ]] || [[ "$file" == *"latest.yml" ]] || [[ "$file" == *"Setup"*".exe.blockmap" ]]; then
              echo "Uploading $file..."
              gh release upload v$VERSION "$file" --clobber
            fi
          done
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish Release
        shell: bash
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "Publishing release v$VERSION..."
          gh release edit v$VERSION --draft=false --latest
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  submit-winget:
    name: Submit WinGet update
    needs: build-windows
    if: github.repository == 'LuqP2/Image-MetaHub' && startsWith(github.ref, 'refs/tags/v')
    runs-on: windows-latest
    permissions:
      contents: read

    steps:
      - name: Download WinGetCreate
        shell: pwsh
        run: |
          Invoke-WebRequest `
            -Uri "https://github.com/microsoft/winget-create/releases/download/v1.12.13.0/wingetcreate.exe" `
            -OutFile "wingetcreate.exe"

          $actualHash = (Get-FileHash .\wingetcreate.exe -Algorithm SHA256).Hash
          $expectedHash = "24042BD37915805615E6CF969AC57C6439124C3FE85823327F5F3FB24BD9FFEA"

          if ($actualHash -ne $expectedHash) {
            throw "WinGetCreate integrity check failed."
          }

      - name: Submit WinGet manifest
        shell: pwsh
        env:
          WINGET_CREATE_GITHUB_TOKEN: ${{ secrets.WINGET_CREATE_GITHUB_TOKEN }}
        run: |
          $version = "${{ github.ref_name }}" -replace '^v', ''
          $installerUrl = "https://github.com/${{ github.repository }}/releases/download/${{ github.ref_name }}/ImageMetaHub-Setup-$version.exe"
          $installerSpec = "$installerUrl|x64|user"

          .\wingetcreate.exe update LuqP2.ImageMetaHub `
            --version $version `
            --urls $installerSpec `
            --submit

  build-macos:
    name: Build macOS
    runs-on: macos-latest
    needs: build-windows
    permissions:
      contents: write
      packages: write
    env:
      IMH_LICENSE_SERVER_URL: ${{ vars.LICENSE_SERVER_URL }}
      IMH_LICENSE_PUBLIC_KEY: ${{ vars.LICENSE_SERVER_PUBLIC_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Configure public license client
        run: node scripts/configureLicenseClient.mjs

      - name: Build macOS installer
        run: |
          echo "Building macOS installer..."
          npm run build
          npx electron-builder --mac --publish=never
          echo "macOS build completed!"
          ls -la dist-electron/
        continue-on-error: true

      - name: Upload macOS assets
        if: success()
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "Uploading macOS assets for v$VERSION"
          if [ -d "dist-electron" ]; then
            for file in dist-electron/*; do
              if [[ "$file" == *".dmg" ]] || [[ "$file" == *"mac.yml" ]] || [[ "$file" == *".dmg.blockmap" ]]; then
                echo "Uploading $file..."
                gh release upload v$VERSION "$file" --clobber
              fi
            done
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    needs: build-windows
    permissions:
      contents: write
      packages: write
    env:
      IMH_LICENSE_SERVER_URL: ${{ vars.LICENSE_SERVER_URL }}
      IMH_LICENSE_PUBLIC_KEY: ${{ vars.LICENSE_SERVER_PUBLIC_KEY }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Configure public license client
        run: node scripts/configureLicenseClient.mjs

      - name: Build Linux installer
        run: |
          echo "Building Linux installer..."
          npm run build
          npx electron-builder --linux --publish=never
          echo "Linux build completed!"
          ls -la dist-electron/
        continue-on-error: true

      - name: Upload Linux assets
        if: success()
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          echo "Uploading Linux assets for v$VERSION"
          for file in dist-electron/*; do
            if [[ "$file" == *".AppImage" ]] || [[ "$file" == *"linux.yml" ]] || [[ "$file" == *".AppImage.blockmap" ]]; then
              echo "Uploading $file..."
              gh release upload v$VERSION "$file" --clobber
            fi
          done
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
